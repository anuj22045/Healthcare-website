<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MediConnect - Signup</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes floating {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-8px);
            }
        }

        /* Border ray animation */
        @keyframes borderAnimation {
            0% {
                border-image-source: linear-gradient(0deg, red, transparent);
            }

            25% {
                border-image-source: linear-gradient(90deg, rgb(154, 255, 1), transparent);
            }

            50% {
                border-image-source: linear-gradient(180deg, rgb(1, 255, 166), transparent);
            }

            75% {
                border-image-source: linear-gradient(270deg, rgb(18, 41, 255), transparent);
            }

            100% {
                border-image-source: linear-gradient(360deg, rgb(245, 22, 241), transparent);
            }
        }

        .gradient-text {
            background: linear-gradient(90deg, #ff0000, #ff7300, #ffeb00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Login link hover effect */
        .login-link {
            transition: color 0.3s ease-in-out, text-shadow 0.3s ease-in-out;
        }

        .login-link:hover {
            color: rgb(7, 253, 245);
            text-shadow: 0 0 10px cyan;
        }

        /* Button pulse effect */
        @keyframes pulse {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.05);
            }

            100% {
                transform: scale(1);
            }
        }

        /* Glow on input focus */
        .input-glow:focus {
            box-shadow: 0 0 15px rgba(0, 255, 255, 0.6);
            border-color: rgb(12, 241, 149);
            transform: scale(1.05);
        }

        /* Applying animations */
        .animate-fadeIn {
            animation: fadeIn 1s ease-out;
        }

        .floating-box {
            animation: floating 5s infinite ease-in-out;
        }

        .glowing-border {
            border: 4px solid transparent;
            border-image-slice: 1;
            animation: borderAnimation 3s infinite linear;
        }

        .hover-pulse:hover {
            animation: pulse 0.4s ease-in-out;
        }
    </style>
</head>

<body class="bg-cover bg-center bg-no-repeat h-screen" style="background-image: url('/images/bg-image.jpeg');">
    <%- include('header') %>

        <div class="flex justify-center items-center h-screen bg-gray-900 bg-opacity-10">
            <div
                class="bg-white bg-opacity-20 backdrop-blur-lg p-8 rounded-lg shadow-lg w-full max-w-md glowing-border animate-fadeIn floating-box">
                <div class="text-center mb-6">
                    <h1 class="text-2xl font-bold gradient-text">Welcome to MediCare</h1>
                    <p class="text-gray-200">Signup to MediCare</p>
                </div>

                <% if (locals.errorMessage) { %>
                    <div class="bg-red-100 text-red-700 p-3 rounded-md mb-4">
                        <%= errorMessage %>
                    </div>
                    <% } %>

                        <form action="/login" method="POST">
                            <div class="mb-4">
                                <label for="fullname" class="block font-semibold text-white">Full Name</label>
                                <input type="text" id="fullname" name="fullname" placeholder="Enter your fullname"
                                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none bg-transparent text-white placeholder-gray-300 transition duration-300 ease-in-out transform focus:scale-105 input-glow"
                                    required>
                            </div>

                            <div class="mb-4">
                                <label for="username" class="block font-semibold text-white">Username</label>
                                <input type="text" id="username" name="username" placeholder="Enter your username"
                                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none bg-transparent text-white placeholder-gray-300 transition duration-300 ease-in-out transform focus:scale-105 input-glow"
                                    required>
                            </div>

                            <div class="mb-4">
                                <label for="password" class="block font-semibold text-white">Password</label>
                                <input type="password" id="password" name="password" placeholder="Enter your password"
                                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none bg-transparent text-white placeholder-gray-300 transition duration-300 ease-in-out transform focus:scale-105 input-glow"
                                    required>
                            </div>

                            <div class="mb-4">
                                <label for="confirmPassword" class="block font-semibold text-white">Confirm
                                    Password</label>
                                <input type="password" id="confirmPassword" name="confirmPassword"
                                    placeholder="Confirm your password"
                                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none bg-transparent text-white placeholder-gray-300 transition duration-300 ease-in-out transform focus:scale-105 input-glow"
                                    required>
                            </div>

                            <button type="submit"
                                class="w-full bg-green-500 text-white py-2 rounded-md hover:bg-green-600 transition duration-300 shadow-lg hover:shadow-green-500/50 hover-pulse">
                                Sign Up
                            </button>
                        </form>

                        <div class="text-center mt-4">
                            <p class="text-gray-200">Already have an account?
                                <a href="/login" class="text-blue-800 login-link">Login</a>
                            </p>
                        </div>
            </div>
        </div>
</body>

</html>