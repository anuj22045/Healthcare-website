<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Header</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-20px);
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .animate-fade {
            animation: fadeIn 0.8s ease-in-out;
        }

        .animate-slide {
            animation: slideIn 0.6s ease-out;
        }
    </style>
</head>

<body>
    <header class="bg-blue-600 text-white shadow-md h-16 flex items-center animate-fade">
        <div class="container mx-auto flex justify-between items-center py-4 px-6">
            <a href="/"
                class="text-2xl font-bold flex items-center hover:text-gray-200 transition duration-300 animate-slide">
                <span>Medi</span>
                <img src="/images/header-logo.png" alt="Logo" class="h-6 w-6 mx-1">
                <span>Care</span>
            </a>

            <nav class="hidden md:flex space-x-6 animate-fade">
                <a href="/" class="hover:text-gray-300 transition-transform duration-300 hover:scale-105">Home</a>
                <a href="/medicines"
                    class="hover:text-gray-300 transition-transform duration-300 hover:scale-105">Medicines</a>
                <a href="/tests" class="hover:text-gray-300 transition-transform duration-300 hover:scale-105">Lab
                    test</a>
                <a href="/doctors"
                    class="hover:text-gray-300 transition-transform duration-300 hover:scale-105">Doctors</a>
                <a href="/contact"
                    class="hover:text-gray-300 transition-transform duration-300 hover:scale-105">Contact</a>
            </nav>

            <div class="relative w-1/3 hidden md:block animate-slide">
                <input type="text" placeholder="Search medicines, health products..."
                    class="w-full px-4 py-2 text-gray-900 rounded-full border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:outline-none transition-transform duration-300 hover:scale-105">
                <button
                    class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-600 hover:text-gray-800 transition duration-300">
                    🔍
                </button>
            </div>

            <div class="flex space-x-4 animate-slide">
                <a href="/login"
                    class="bg-white text-blue-600 px-4 py-2 rounded-md hover:bg-gray-100 transition-transform duration-300 hover:scale-105">
                    Login
                </a>
                <a href="/cart"
                    class="bg-yellow-400 text-black px-4 py-2 rounded-md hover:bg-yellow-500 transition-transform duration-300 hover:scale-105">
                    🛒 Cart (<%= typeof cartCount !=='undefined' ? cartCount : 0 %>)
                </a>
            </div>

            <button
                class="md:hidden text-white text-2xl hover:text-gray-200 transition duration-300 animate-slide">☰</button>
        </div>
    </header>
</body>

</html>