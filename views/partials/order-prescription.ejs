<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>


    <div class="w-full md:w-1/2 p-8">
        <h3 class="text-lg font-medium text-gray-700 mb-6">How does this work?</h3>
        <div class="space-y-6">
            <% if (steps && steps.length) { %>
                <% steps.forEach((step, index)=> { %>
                    <div class="flex items-start">
                        <div
                            class="flex-shrink-0 bg-blue-100 text-blue-600 font-bold rounded-full h-8 w-8 flex items-center justify-center mr-4">
                            <%= index + 1 %>
                        </div>
                        <p class="text-gray-700">
                            <%= step %>
                        </p>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <p class="text-gray-700">No steps available</p>
                            <% } %>
        </div>
    </div>

</body>

</html>